@{
    ViewData["Title"] = "Trang Chủ";
}

<section class="hero text-center py-6">
    <div class="container">
        <h1 class="hero-title">Tâm Trạng — Bạn không đơn độc</h1>
        <p class="hero-sub">Một không gian yên tĩnh, ấm áp để học, chia sẻ và tìm sự hỗ trợ về rối loạn trầm cảm nặng.</p>
    </div>
</section>

<section class="container mt-5">
    <div class="row align-items-stretch">
        <div class="col-lg-6 mb-4">
            <div class="neon-mockup">
                <div class="mockup-inner">
                    <div class="mockup-side">
                        <div class="tabs">Editor</div>
                        <ul class="saved">
                            <li>New tab</li>
                            <li>Console</li>
                        </ul>
                    </div>
                    <div class="mockup-main">
                        <pre class="code-sample">-- Hơi thở, tĩnh lặng
local function breath()
    -- Hãy thử hít vào 4 giây, giữ 4, thở ra 6
end
</pre>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-lg-6">
            <div class="content-cards">
                <div class="card mb-4 bg-transparent border-0">
                    <div class="card-body text-light">
                        <h3 class="card-title">Tài nguyên & Bài viết</h3>
                        <p>Khám phá bài viết hướng dẫn tự chăm sóc, video hướng dẫn và tài liệu được chọn lọc để giúp bạn đi qua những ngày khó khăn.</p>
                        <a asp-controller="Home" asp-action="Resources" class="btn btn-outline-light">Xem Tài Nguyên</a>
                    </div>
                </div>

                <div class="card mb-4 bg-transparent border-0">
                    <div class="card-body text-light">
                        <h3 class="card-title">Nhóm hỗ trợ</h3>
                        <p>Kết nối với cộng đồng an toàn, tham gia các buổi chia sẻ hoặc tìm nhóm hỗ trợ theo khu vực.</p>
                        <a asp-controller="Home" asp-action="Index" class="btn btn-outline-light">Tìm Nhóm</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@if (ViewBag.FeaturedArticles != null && ViewBag.FeaturedArticles.Count > 0)
{
    <section class="container mt-5">
        <h2 class="mb-4">Bài viết nổi bật</h2>
        <div class="row">
            @foreach (var article in ViewBag.FeaturedArticles)
            {
                <div class="col-md-4">
                    <div class="card article-card bg-dark-card text-light mb-3">
                        @if (!string.IsNullOrEmpty(article.ImageUrl))
                        {
                            <img src="@article.ImageUrl" class="card-img-top" alt="@article.Title">
                        }
                        <div class="card-body">
                            <h5 class="card-title">@article.Title</h5>
                            <p class="card-text">@article.Content.Substring(0, Math.Min(120, article.Content.Length))...</p>
                            <p class="text-muted"><small>Đăng bởi @article.Author vào @article.CreatedDate.ToString("dd/MM/yyyy")</small></p>
                        </div>
                    </div>
                </div>
            }
        </div>
    </section>
}

@section Scripts {
    <script>
        // any homepage specific scripts
    </script>
}